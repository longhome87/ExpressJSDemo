extends layout

block content
  .container-fluid
    .row
      each row in result.rows
        .product-item-container.col-xl-3.col-lg-4.col-md-6
          .card
            .product-item
              a(data-fancybox='' href='uploads/' + row.image)
                img.card-img-top(src='uploads/' + row.image)
            .card-body
              h5.card-title #{row.name}
              p.card-text
                | Some quick example text to build on the card title and make up the bulk of the card&apos;s content.
              //- let price = parseInt(row.price).toLocaleString('vn-VN', { style: 'currency', currency: 'VND' });
              - let price = parseInt(row.price).toLocaleString('vn-VN');
              h4.product-price.float-left #{price.replace(',','.')}
                span 
                  u đ
              a#btnAddToCart(href='' role='button' onclick='return addToCart(' + row.id + ');').btn.btn-success.float-right Add To Cart
